import 'package:flutter/material.dart';

class AppColors {
  // Surfaces
  static const background = Color(0xFFF8F9FF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceContainerLow = Color(0xFFEFF4FF);
  static const surfaceContainer = Color(0xFFE5EEFF);
  static const border = Color(0xFFE2E8F0);
  static const outline = Color(0xFF727785);

  // Primary blues
  static const primary = Color(0xFF0058BE);
  static const primaryContainer = Color(0xFF2170E4);
  static const primaryLight = Color(0xFFD8E2FF);
  static const onPrimary = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF0B1C30);
  static const textSecondary = Color(0xFF424754);
  static const textMuted = Color(0xFF727785);

  // Status / semantic
  static const overdue = Color(0xFFBA1A1A);
  static const overdueLight = Color(0xFFFFDAD6);
  static const upcoming = Color(0xFF825100);
  static const upcomingLight = Color(0xFFFFDDB8);
  static const done = Color(0xFF10B981);
  static const doneLight = Color(0xFFD1FAE5);

  // Priority
  static const high = Color(0xFFBA1A1A);
  static const medium = Color(0xFF825100);
  static const low = Color(0xFF10B981);
}
